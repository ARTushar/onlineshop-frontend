import * as ActionTypes from './ActionTypes';


const Wishlist = (state = {
    products: [],
}, action) => {
    switch(action.type){
        
        
        default:
            return state;
    }
}

export default Wishlist;